// We're keeping the same import style but this file now uses JavaScript instead of TypeScript
import { useToast as useToastOriginal } from "@/components/ui/use-toast";

export const useToast = useToastOriginal;
